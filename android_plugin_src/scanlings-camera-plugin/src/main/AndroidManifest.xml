<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <provider
            android:name="com.scanlings.camera.ScanlingsFileProvider"
            android:authorities="${applicationId}.scanlings.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/scanlings_file_paths" />
        </provider>
    </application>

</manifest>
